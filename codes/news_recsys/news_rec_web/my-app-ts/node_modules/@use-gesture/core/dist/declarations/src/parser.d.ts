import { GestureHandlers, UserGestureConfig } from './types';
export declare function parseMergedHandlers(mergedHandlers: GestureHandlers, mergedConfig: UserGestureConfig): {
    handlers: {};
    config: UserGestureConfig;
    nativeHandlers: any;
};
